# Pi Dashboard Configuration
# Copy this file to .env and edit values

# Server port
PORT=3001

# Admin token for protected endpoints (config changes)
# Generate a strong random token for production!
# Leave empty to disable auth (dev mode only)
ADMIN_TOKEN=

# CORS allowed origins (comma-separated)
# Use * for all origins (default, ok for local network)
# Use specific origins in production: https://dash.example.com
CORS_ORIGINS=*

# Path to settings config file
SETTINGS_CONFIG=./settings.json
